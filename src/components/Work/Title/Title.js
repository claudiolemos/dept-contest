import React from 'react';

import {Section, TitleText, Button} from './Title.style.js';

function Title(props) {
    return (
        <Section>
            <TitleText>Work</TitleText>
            <Button>View Case</Button>
        </Section>
    );
  }
  
  export default Title;
  